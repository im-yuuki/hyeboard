apiVersion: v1
kind: Service
metadata:
  name: hyeboard-api
  labels:
    app.kubernetes.io/name: hyeboard-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hyeboard-api
  ports:
    - name: http
      port: 80
      targetPort: http
